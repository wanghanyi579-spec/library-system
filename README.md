# Library Management System

A simple command-line library management system written in Python.

## Features
- Book management (add, delete, search, list)
- Member management (register, delete, update, list)
- Borrowing operations (borrow, return, renew)
- Statistics and data backup

## How to Run
```bash
python 3.0.py
